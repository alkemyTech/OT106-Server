const path = require('path')


const IMAGE_NOT_FOUND = 'image dont found'
const NAME_NOT_FOUND = "name dont found"
const CONTENT_NOT_FOUND = "content not found"
const ACITIVITY_NOT_FOUND = "there is no activity with that id"
const IMAGE_PATH = '../public/images/'


module.exports = {
    IMAGE_NOT_FOUND,
    NAME_NOT_FOUND,
    CONTENT_NOT_FOUND,
    ACITIVITY_NOT_FOUND,
    IMAGE_PATH
}